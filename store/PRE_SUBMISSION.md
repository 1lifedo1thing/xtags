# 提交前检查

## P0：建议关闭这些缺口后再送审

- [x] **显著披露与同意**：0.1.5 已加入未预选的勾选项和“同意并启用”，同意版本由后台和内容脚本共同验证。新装/旧版本升级默认禁止请求，支持撤回、取消重试和丢弃迟到结果。随包本地隐私政策可直接打开。
- [x] **隐私政策公开 URL**：GitHub Pages 已从 main /docs 发布。2026-09-20 使用无登录凭证的 HTTPS 请求验证中英文政策、主页、支持页及静态资源均返回 200，内容与源码一致。地址见 GITHUB_PAGES.md。
- [ ] **真实联调与审核访问**：完成真实账号/API key 的端到端验证，准备专用审核 key、有效额度和可访问的文本帖子；仅在商城私有字段提供凭证。
- [x] **实际时间线截图**：用户提供并授权公开使用的本人实际标签截图已加入，包含“挑拨 0.68”标签。已制作 1280 × 800 商城版本并嵌入中英文 README；完整端到端验证仍单独待完成。

## P1：强烈建议同步完善

- [ ] **明确敏感内容范围**：当前扫描已渲染 DOM，不区分公开和受保护帖子，也不是仅当前视口内内容。0.1.5 已明确披露；进一步改进是限制不希望外发的内容/提供按次处理方式。不得在文案中写“只处理公开帖子”。
- [ ] **数据最小化**：评估分类是否真的需要作者 handle；如果不需要，删除上传字段并同步修改政策、权限说明和截图。
- [x] **新用户引导**：设置页已加入“获取 TypeSafe API key”链接，并在同意前明确自备 key 和服务商可能收费。
- [x] **误判文案**：同意前展示 AI 判断可能有误的提示；商店详细说明保留同等说明。
- [ ] **key 隔离**：当前内容脚本读取包含 key 的配置，storage.local 默认可被内容脚本访问。可将敏感存储限制到可信扩展上下文，通过后台提供 hasKey/设置接口。未发现普通网页可直接读取 key 的证据；这是加强隔离的建议。
- [ ] **主页链接**：隐私网站上线后，把 manifest.homepage_url 从个人首页改为项目页；如运行包已上传过，应按版本规则递增并重打包。
- [ ] **删除和停用路径**：用户需要“暂停→清缓存→清空 key”才能避免缓存即时重建。帮助页已解释，产品可增加显式删除本地数据按钮。

## 已通过的静态检查

- [x] Manifest V3，默认 API 域名权限限于 TypeSafe；可选 HTTPS 域名权限仅在保存自定义地址时按所选域名请求。
- [x] 内容脚本仅匹配 x.com / twitter.com；无全站匹配、history、cookies、tabs、webRequest 权限。
- [x] 没有运行时下载执行 JS、eval 或 Function 构造器；所有执行逻辑随包分发。
- [x] API 通过 HTTPS；存储使用 local 而非 sync；无开发者遥测端点。
- [x] 中英界面与本地化描述已包含；语言切换复用概率缓存。
- [x] MIT LICENSE 已加入商城候选 ZIP；manifest.json 位于包根目录。
- [x] 31 项后台/语言测试、30 项真实 DOM 回归已在 0.1.5 开发过程中通过；服务商和浏览器 API 由测试夹具模拟。

## 需人工完成的真实环境测试

| 场景 | 验收标准 | 结果 |
| --- | --- | --- |
| 新装、无 key | 无分类请求；语言正确；引导可理解 | 待测 |
| 有效 key + 首页/详情页 | 收到实际响应，标签贴到对应帖子 | 待测 |
| 回复、引用、转帖、长文本 | 无正文/作者错配；跳过回复符合设置 | 待测 |
| 快速滚动、打开两个标签页 | 节点复用无错位；同帖去重 | 待测 |
| 请求中暂停/清缓存/换 key | 停止队列，旧结果不重新贴回 | 待测 |
| 401、429、离线、恢复网络 | 错误清楚、重试有限、恢复可操作 | 待测 |
| 中英切换、重开 popup | 选择保留，标签即时更新，不重复请求 | 待测 |
| 深浅色、窄窗口 | 标签和面板可读，无遮挡 | 待测 |
| 卸载/清数据 | 本地数据清理与政策描述一致 | 待测 |

## 发布者与后台

- [ ] Chrome Web Store 开发者账户注册、注册费及邮箱验证完成；已启用两步验证。
- [ ] 确认公开名称、支持邮箱、需要时的主体/地址/地区信息。不要填入不真实的身份或 trader 声明。
- [ ] 如后台询问第三方付费访问/额外付款，按所选服务商的实际收费方式如实填报（默认 TypeSafe），不能只因扩展代码免费就宣称所有功能免费。
- [ ] 核对数据申报、listing、产品 UI 与隐私政策相互一致。
- [ ] 上传至少一张真实功能截图与必需小宣传图；不上传 key、私人帖子或开发者控制台秘密。
- [ ] 初次建议使用受控测试或审核通过后手动发布；不要把提交成功等同于批准。

参考：[账户](https://developer.chrome.com/docs/webstore/set-up-account)、[两步验证](https://developer.chrome.com/docs/webstore/program-policies/two-step-verification)、[发布流程](https://developer.chrome.com/docs/webstore/publish)。

- [ ] 自定义兼容 API 真机验证：首次授权、拒绝、撤销、换域名、同域名换路径、切回官方及错误响应。测试使用的域名和 key 仅放审核后台私有字段。
